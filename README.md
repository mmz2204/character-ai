# 角色定妆照提示词生成器 Pro

一个纯前端单文件工具（`index.html`），用于根据角色设定生成 AI 绘图用的定妆照提示词。

## 使用

直接用浏览器打开 `index.html` 即可，无需安装依赖、无需构建、无后端。

## 特性

- 单文件、零依赖、离线可用
- 中文界面，面向角色定妆照 / 人像提示词场景
- 本地运行，不上传任何数据

## 技术栈

- 原生 HTML / CSS / JavaScript
- 无框架、无构建步骤
